# Copia este archivo como .env y llena los valores
# cp .env.example .env

# Clave secreta para las sesiones (genera una larga y aleatoria)
SESSION_SECRET=cambia-esto-por-una-clave-muy-segura-y-larga

# Puerto del servidor
PORT=3000

# Ambiente
NODE_ENV=production
